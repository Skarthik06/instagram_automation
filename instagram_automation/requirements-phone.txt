# Business-SK PHONE server — light deps ONLY (Option A engagement half).
# Deliberately excludes torch / transformers / playwright / rembg / onnxruntime / docling /
# opencv / pillow — those heavy content-generation deps stay on the PC.
# Install inside proot Ubuntu:  pip install -r requirements-phone.txt
fastapi>=0.111,<0.116
uvicorn[standard]>=0.30.1
pydantic>=2.9.2,<3
python-dotenv>=1.0.1
requests>=2.32
openai>=2.0                 # only used if a rule opts into AI-rephrased replies (grounded)
psycopg[binary]>=3.1,<4     # Postgres driver (binary wheel works on glibc / proot Ubuntu)
cryptography>=44            # decrypts the IG page tokens with .ragskey
